<%= simple_form_for([@tournament, @game, @statistic]) do |f| %>
  <%= f.association :user, collection: team.players_in_tournament(@tournament)%>
  <%= f.association :action %>
  <%= f.button :submit, "Save Statistic"%>
<% end %>
