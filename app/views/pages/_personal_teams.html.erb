<% teams.each do |team| %>
  <%= link_to team_path(team) do %>
    <div class="card-info-teams" style="margin-top: 10px;">
      <h1 style="font-size: 24px; text-align: left; color: #C7EAF5; text-decoration: none; line-height: 60px;"><%= team.name %></h1>
    </div>
  <% end %>
<% end %>
